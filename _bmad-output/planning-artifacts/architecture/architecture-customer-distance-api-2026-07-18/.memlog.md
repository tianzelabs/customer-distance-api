---
scope: Customer Distance API backend service (FR-1-FR-14, prd.md)
purpose: build-substrate
altitude: feature
updated: 2026-07-18T21:19
---

- (event) Create intent; sources: prd.md (status final, FR-1-FR-14) and addendum.md (parameterized-query constraint). User requires full architecture process (no quick-dev), traceability to FR-1-FR-14, addendum constraint elevated to mandatory AD, project stays small/offline/homework-scale, no implementation code this session
- (decision) Working mode: Coaching path confirmed explicitly by user, for the same full-process-preservation reason as the PRD run
- (decision) Deliverable scope: ARCHITECTURE-SPINE.md only, no separate HTML solution design doc for now (revisit at Finalize if it would add comparison value). Spine must be: epic/story-ready, detailed enough to leave no basic technical decisions open at implementation time, proportionate to a small homework backend, traceable to FR-1-FR-14, and readable by a human evaluator
- (version) Web-verified (2026-07-18): Node.js 24 is current Active LTS (26 is Current/not-yet-LTS until Oct 2026, 22 is Maintenance-only); pg (node-postgres) 8.22.0 current; node-pg-migrate is the standard lightweight Postgres-native migration tool (no ORM coupling); node:test stable since Node 20, zero-dependency; Express vs Fastify: Fastify faster/more schema-driven and increasingly default for greenfield 2026, Express wins on ecosystem familiarity/simplicity for simple APIs/internal tools
- (decision) AD candidate: Design paradigm = light layered architecture: HTTP route/handler -> application/service -> data-access/repository -> PostgreSQL. Binds: all FRs. Prevents: domain logic (Haversine, rounding, deterministic sort) leaking into route handlers, and seed/API using divergent DB access paths. Rule: route handlers call service functions only; service functions call repository functions only; repository functions are the only place SQL is executed
- (decision) AD candidate: seed process is a standalone application entrypoint (not routed through HTTP/application layer) but MUST reuse the same shared DB connection/Pool module and the same parameterized repository/data-access functions as the API. Prevents: duplicated/divergent SQL between seed script and API
- (decision) Stack confirmed by user: Node.js 24 LTS; TypeScript; Express 5 (verified 5.2.1, Active support phase per expressjs.com LTS policy); pg/node-postgres via Pool, no ORM/query-builder; node-pg-migrate; PostgreSQL via Docker Compose. Test runner changed from node:test (my proposal) to Vitest (4.1.x, not the 5.0 beta) - user reason: better TS DX, toBeCloseTo-style tolerance assertions for Haversine, cleaner integration setup/teardown, and matches the toolchain on the parallel Superpowers branch so the harness comparison isn't skewed by differing test tooling
- (constraint) Mandatory architecture rules from user: (1) Express app construction separated from server.listen() so the app is importable unbound in integration tests; (2) DB Pool sourced from exactly one shared module; (3) route handlers contain zero raw SQL; (4) seed and API share the same repository/data-access layer (no duplicate SQL); (5) Haversine function is pure - no DB or HTTP dependency; (6) migrations are versioned and explicitly rollback-capable (up/down); (7) no DI framework/container/complex domain layer - stay proportionate to homework scale
- (version) Web-verified (2026-07-18): Express latest 5.2.1, Active support phase, production-recommended. Vitest latest stable 4.1.10 (5.0.0-beta.1 exists but not stable - pin to 4.x). TypeScript: 6.0.2 is the last JS-based-compiler release (mature tooling ecosystem); 7.0.2 is a brand-new Go-based full rewrite, only ~8 days old at time of research - flagged as an open version-pin question for the user rather than silently assumed
- (decision) TypeScript pinned to exact 6.0.2 (not a floating range) - user reason: 7.0 is a brand-new Go-rewrite compiler, tooling/editor/Vitest/ESLint compat risk not worth it for a reproducibility-first homework project. package.json must pin the exact version (no ^ or ~), installs via npm ci for reproducibility. This is now a Stack row + a documented packaging/install convention
- (decision) Data model finalized: customers(id BIGSERIAL PK, name TEXT NOT NULL, telepules TEXT NOT NULL, lat DOUBLE PRECISION NULL, lon DOUBLE PRECISION NULL, budget INTEGER NULL, note TEXT NULL, country_code VARCHAR(2) NULL, UNIQUE(name, telepules)). Added user-requested DB-level CHECK constraints: lat BETWEEN -90 AND 90 (or null), lon BETWEEN -180 AND 180 (or null), and lat/lon must be both-null or both-not-null (paired nullity). No created_at/updated_at/UUID/audit columns. distanceKm is explicitly NOT a DB column - computed API response field only
- (decision) Module boundary refinement: geocoding/ split into normalizeTown.ts (pure normalization function, independently unit-testable) and townReference.ts (static reference data) - user reason: normalization algorithm and reference data are different responsibilities. Formal per-layer responsibility rules: route=HTTP I/O only; service=distanceKm calc + rounding + deterministic sort; repository=DB I/O and row-to-domain mapping ONLY, must not contain Haversine or sort business logic; seed reuses normalizeTown/townReference + repository upsert function
- (constraint) AD refinement on parameterized-query rule (addendum.md / FR-2,6,7): every dynamic SQL value must go through a parameterized placeholder; static, parameter-free SELECT statements are explicitly exempt from an artificial placeholder requirement (clarifies the rule doesn't demand placeholders where there is no dynamic value)
- (decision) Runtime data flow confirmed: seed uses one parameterized upsert per record (15 records - no bulk loader/batch infra needed). by-distance processing is application-side: repository.findAll() -> compute distanceKm per row -> round to 1 decimal at response-assembly time -> sort (non-null distanceKm ascending, tie-break name ascending; null-distance group at the end, tie-break name ascending within that group)
- (constraint) Integration test database rule: tests run against real PostgreSQL but a SEPARATE test database/connection string from dev data (never touches dev data). Test lifecycle: apply/run migrations, load controlled fixture data, deterministically re-runnable, close Pool connections after the suite
- (decision) Naming correction: 'telepules' is NOT accented Hungarian - it is the exact ASCII field name fixed by the original homework spec. It stays 'telepules' consistently across DB column, TS type, and API JSON field (no translation to e.g. 'town' or accented form)
- (decision) Repository DB->domain mapping responsibilities confirmed: country_code -> countryCode; Postgres numeric/string wire representations -> correct TS types. Specifically: pg returns COUNT(*) as a string - repository must explicitly convert to number and validate the result is finite/safely representable before returning it (guards FR-6's 'real DB query, not hardcoded' contract at the type-safety level)
- (constraint) AD candidate: centralized Express 5 error-handling middleware (not per-route try/catch). Fixed client error shape on unexpected/DB errors: {"error":{"message":"Internal server error"}}, HTTP 500. Client response MUST NEVER include raw SQL error text, connection string, stack trace, or any DB/env secret. Actual error logged server-side via console.error with context. No custom error-class hierarchy or error-code taxonomy needed at this scale
- (constraint) Logging convention confirmed: console.warn/console.error only, with consistent bracket prefixes [seed]/[api]/[database]. Example: '[seed] Unknown town: "Example" - lat/lon set to null' (realizes FR-5). Logs must never contain a password or full connection string
- (constraint) AD candidate: config/env.ts is the single place env vars are read. Vars: DATABASE_URL (used by API, migration, and seed), TEST_DATABASE_URL (integration tests only), PORT (optional, documented default). Must validate required values and fail-fast with a clear error message on missing/invalid config; no hardcoded passwords. CRITICAL: integration tests must NOT silently fall back to DATABASE_URL when TEST_DATABASE_URL is unset - must fail-stop instead, to guarantee tests can never mutate the dev database
- (event) Consistency Conventions round closed. All Coaching-path decision rounds (paradigm+stack, data model+modules+dataflow, conventions) complete. Moving to Architecture Finalize: distill, reconcile inputs, reviewer gate, triage, close
- (version) Web-verified (2026-07-18): PostgreSQL 18 (18.4) is current stable major; official Docker image tag postgres:18. Note: Postgres 18 changed PGDATA to a version-specific path (/var/lib/postgresql/18/docker) vs prior versions - relevant for docker-compose.yml volume config, implementation-detail not spine-level
- (event) Finalize step 2 (input reconciliation): 2 subagents run against prd.md and addendum.md. addendum.md: no gaps (AD-2 correctly elevates the constraint). prd.md: 4 gaps found - (1) FR-13 MCP mechanism left Deferred despite PRD explicitly delegating that decision to architecture, (2) AD-2's static-SELECT exemption technically diverges from addendum.md's literal 'every DB operation' wording (though it reflects the user's own later architecture-round refinement), (3) FR-4 normalization behavior has no backing AD, (4) no single-source-of-truth rule preventing the Budapest reference coordinate from drifting between townReference.ts and the Haversine constant
- (decision) Applied 4 reconciliation fixes: AD-11 (Postgres MCP via .mcp.json, env-var connection, README documents usage), AD-12 (single normalizeTown() entry point for FR-4), AD-13 (single exported BUDAPEST_REF constant, no drift between townReference.ts and haversine.ts), and addendum.md reworded to scope the parameterized-query mandate to dynamic-value operations with an explicit static-SELECT exemption cross-referencing AD-2. Capability Map and Deferred updated accordingly
- (event) User switched remaining pipeline to autonomous/Fast-path execution: finish architecture Finalize fast, run reviewer gate and autofix non-scope-increasing mechanical issues, then chain into bmad-create-epics-and-stories (Fast path), bmad-check-implementation-readiness, bmad-sprint-planning, and first story implementation (bmad-create-story + bmad-dev-story) with small focused commits and tests/code-review per story. Only stop for: blocking requirement contradictions, scope-increasing decisions, or dangerous/irreversible operations
- (event) Reviewer Gate run: lint_spine.py clean (0 findings, both passes). Rubric walker: PASS, 2 medium + 2 low. Version-verification lens: PASS, 0 blocking (node-pg-migrate version was unpinned, now fixed). Adversarial lens: 7 genuine unit-divergence pairs found, none rejected. Autofixed all as new/tightened ADs (non-scope-increasing): AD-5 explicit DO UPDATE upsert semantics, AD-7 mandates UNIQUE+CHECK constraints in migration + timestamped migration filenames, AD-9 extended with test-DB topology (single postgres:18 instance, two logical DBs), new AD-14 single Customer type ownership, Consistency Conventions tightened (key-omission vs null convention, id bigint coercion), sort rule given id-ascending final tiebreak for full determinism, node-pg-migrate pinned to 8.0.4, PG18 PGDATA note added. Deferred section trimmed of 2 now-resolved bullets. No blockers, no scope increase, no user check-in needed per autonomous-mode instruction
- (event) spine finalized
- (event) Implementation readiness check (bmad-check-implementation-readiness) found status NEEDS WORK: 2 major findings, both fixed - (1) epics.md Story 1.4 now explicitly creates config/env.ts + db/pool.ts (was a forward-dependency on Story 2.2), Story 2.2 reworded to extend not recreate them; (2) AD-11 now names the concrete MCP package (@modelcontextprotocol/server-postgres, verified official/read-only-enforced), Deferred bullet removed. 5 minor findings accepted as-is (informational, non-blocking). Report: _bmad-output/planning-artifacts/implementation-readiness-report-2026-07-18.md
